run the actor:

sudo python3 actor_main.py

Note the server ip/port and actor name are stored in config.txt file.

Important: Because the Actor will run srsRAN UEs, the Actor machine should be configured to be able to run sudo commands without password. 
